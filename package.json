{
  "name": "luxdrive",
  "version": "1.0.0",
  "description": "Backend server for the LuxDrive car catalog.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check script.js",
    "tunnel": "npx localtunnel --port 3000",
    "docker:build": "docker build -t luxdrive ."
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "sqlite3": "^5.1.6"
  }
}
